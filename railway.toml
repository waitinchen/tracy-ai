[project]
name = "lingya-voice-chat"

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/healthz"
restartPolicyType = "on_failure"

[services.backend]
start = "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"

